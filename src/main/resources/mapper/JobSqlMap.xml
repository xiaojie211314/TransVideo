<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lijie.demo.dao.TransCodingMapper">


    <insert id="insertJob" useGeneratedKeys="true" keyProperty="id">
        insert into video_job(srcpath,despath,status)values(#{srcpath},#{despath},0)
    </insert>

    <update id="updateJob">
        update video_job set despath = #{despath}, status=#{status} where id=#{id}
    </update>


    <!--查找未执行的任务-->
    <select id="findJob" resultType="com.lijie.demo.bean.VideoJob">
        select id,srcpath,despath,status from video_job where status=0
    </select>


</mapper>
